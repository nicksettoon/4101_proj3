(load "./s48.scm")
(load "./done.scm")

;;n-ary operators
; +

; -
; *
; /
; =
; eqv?
; equal?
; w that works for rationals

; gcd
(define (gcd . l)
    (cond
        ((null? l) 0)
    )
)

; (gcd)
; (gcd 3)
; (gcd 6 9)
; (gcd 1764 42)


;; if have time
    ; lcm
    ;; n-ary operators
    ; max
    ; min
    ; =
    ; <
    ; >
    ; <=
    ; >=
    ; assq
    ; assv
    ; assoc