(load "./s48.scm")
(load "./done.scm")

;;n-ary operators
; +
; *
; /
    ; b/
; +
; -
; *
; / 
; =
; equal?

; w that works for rationals
;; if have time
    ; lcm
    ;; n-ary operators
    ; max
    ; min
    ; =
    ; <
    ; >
    ; <=
    ; >=
    ; assq
    ; assv
    ; assoc